/**
 * Витрина корпуса: слаги подобраны руками, чтобы первым же нажатием было видно
 * расхождение. Что каждый из них существует, проверяет соседний тест — иначе
 * выпавший из корпуса слаг превратился бы в кнопку, которая молча ничего не делает.
 */
export const chipSlugs = [
  "buckwheat",
  "oats",
  "spaghetti",
  "ground-beef",
  "pollock",
  "apple",
  "parmesan",
];

export const startSlug = chipSlugs[0]!;
