/**
 * СНЯТО С ПОКАЗА, НЕ УДАЛЕНО.
 *
 * Демо переставлено с бэкенда FoodIQ на фронтенд (`foodiq-front`) и доведено
 * не до конца. Готово: три яруса, ворота на настоящих проверках репозитория
 * (границы слоёв, клиент API против `schema.yml`, храповик долга), измерения,
 * тексты ru/en, восемь правок.
 *
 * Не готово — цепочка ревьюеров:
 * - записаны возражения трёх звеньев из четырёх (`card-fetches`,
 *   `own-default`, `double-suspense`); у `untested-lib` возражение звена
 *   «цена» получено, но не вставлено;
 * - `card-fetches` переименован из `three-jobs` и ждёт своего текста в
 *   `messages/*.json` и правильного `files` в `diffs.ts`;
 * - `gates.test.ts` и `e2e/guardrails.spec.ts` под новые идентификаторы
 *   поправлены не полностью.
 *
 * Уроки, которые нельзя потерять при доработке: первое звено дало сильнейшее
 * возражение на правку, которую и так ловят границы (я неверно его настроил —
 * сказал, что границы уже прошли, хотя импорт соседней фичи там был), поэтому
 * взято его третье возражение. Второе звено возразило не про обёртки над
 * `useGetUserGoals`, как я ожидал, а про дефолт дневной цели, написанный
 * четырьмя способами. Правку под возражение меняли, а не возражение под правку.
 *
 * Чтобы вернуть: добавить `"guardrails"` обратно в `demoIds`
 * (`src/entities/case/model/schema.ts`), в таблицу `DEMOS`
 * (`src/widgets/demos/ui/demo-slot.tsx`) и в `demos` кейса
 * `src/content/cases/agent-harness.ts`. Снять `.skip` с `e2e/guardrails.spec.ts`.
 */
export { GuardrailsDemo } from "./ui/guardrails-demo";
