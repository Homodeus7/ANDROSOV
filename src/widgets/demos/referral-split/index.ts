/**
 * СНЯТО С ПОКАЗА, НЕ УДАЛЕНО.
 *
 * Причина не техническая: код рабочий и тесты зелёные. На том проекте расчёт
 * реферальных долей делал бэкенд, а на фронте была вёрстка и несложная логика.
 * Демо читается как заявка на авторство расчёта, которой не было.
 *
 * Чтобы вернуть: добавить `"referral-split"` обратно в `demoIds`
 * (`src/entities/case/model/schema.ts`), в таблицу `DEMOS`
 * (`src/widgets/demos/ui/demo-slot.tsx`) и в `demos` кейса
 * `src/content/cases/web3-terminal.ts`. Снять `.skip` с
 * `e2e/referral-split.spec.ts`.
 *
 * Возвращать — только если найдётся честная рамка: например «разбирался, как
 * это устроено у нас на бэке, и собрал то же самое на фронте, чтобы понять».
 */
export { ReferralSplitDemo } from "./ui/referral-split-demo";
