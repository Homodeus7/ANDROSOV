export type ReferralSplitStrings = {
  pool: string;
  team: string;
  share: string;
  payout: string;
  subtotal: string;
  paid: string;
  drift: string;
  exact: string;
  hint: string;
  note: string;
};
