// Снимок для перелёта заголовка лежит отдельно от `shared/motion` намеренно.
// Меряет он голым DOM, GSAP ему не нужен, а бочка motion ре-экспортирует
// `gsap.ts` с его `registerPlugin` — сайд-эффектом, который не вытрясается.
// Пока `captureFlip` жил там, командная палитра из корневого макета тянула
// библиотеку на каждую страницу, включая резюме, где анимации нет вообще
export { captureFlip, consumeFlip } from "./model/flip-store";
